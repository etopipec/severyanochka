name: Build
on:
  push:
    branches:
      - main
jobs:
  build-only:
    runs-on: severyanochka-label
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Install deps
        run: npm install
      - name: Build only
        run: npm run build-only